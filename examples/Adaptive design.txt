Процедура "Adaptive design"

Алгоритм:
Если The device type == Large screen
    Use large background image

    Use grid layout

    Use small UI elements

    Put full main menu

Иначе
    Если The device type == Laptop
        Use medium background image

        Use grid layout

        Use small UI elements

        Put full main menu

    Иначе
        Если The device type == Tablet
            Use medium background image

            Use plain layout

            Use large UI elements

            Put full main menu

        Иначе
            Если не (The device type == Phone)
                ОШИБКА:
                Unexpected case value
                The device type

            Use small background image

            Use plain layout

            Use large UI elements

            Put minimal main menu

Конец процедуры